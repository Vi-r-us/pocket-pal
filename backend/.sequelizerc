"use strict";

const path = require("path");

module.exports = {
  config: path.resolve("src", "config", "env.config.cjs"),
  "migrations-path": path.resolve("src", "migrations"),
  "seeders-path": path.resolve("src", "seeders"),
};
